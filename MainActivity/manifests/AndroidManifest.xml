<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.prajwal.myfirstapp">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- Android 13+ media permissions for vault import -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <!-- Bluetooth permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Share Intent Filters -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <!-- Task Manager Activity -->
        <activity
            android:name=".TaskManagerActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Task Categories Activity -->
        <activity
            android:name=".TaskCategoriesActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Task Trash Activity -->
        <activity
            android:name=".TaskTrashActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Task Detail Activity -->
        <activity
            android:name=".TaskDetailActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Task Stats / Productivity Activity -->
        <activity
            android:name=".TaskStatsActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Task Search Activity -->
        <activity
            android:name=".TaskSearchActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Meetings List Activity -->
        <activity
            android:name=".MeetingsListActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Create / Edit Meeting Activity -->
        <activity
            android:name=".CreateMeetingActivity"
            android:configChanges="orientation|screenSize"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Meeting Detail Activity -->
        <activity
            android:name=".MeetingDetailActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Task Templates Activity -->
        <activity android:name=".TaskTemplatesActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Task Import Activity -->
        <activity android:name=".TaskImportActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Kanban Board Activity -->
        <activity android:name=".KanbanBoardActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Time Block Activity -->
        <activity android:name=".TimeBlockActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Focus Mode Activity -->
        <activity android:name=".FocusModeActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Meeting Analytics Activity -->
        <activity android:name=".MeetingAnalyticsActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Meeting Search Activity -->
        <activity android:name=".MeetingSearchActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Task Manager Settings Activity -->
        <activity android:name=".TaskManagerSettingsActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Notes Activity -->
        <activity
            android:name=".NotesActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Note Folders Home Activity -->
        <activity
            android:name=".NoteFoldersHomeActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:screenOrientation="portrait"
            android:exported="false" />

        <!-- Note Folder Activity -->
        <activity
            android:name=".NoteFolderActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:screenOrientation="portrait"
            android:exported="false" />

        <!-- Notes Archive Activity -->
        <activity
            android:name=".NotesArchiveActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Notes Trash Activity -->
        <activity
            android:name=".NotesTrashActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Tags Manager Activity -->
        <activity
            android:name=".TagsManagerActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Note Editor Activity -->
        <activity
            android:name=".NoteEditorActivity"
            android:configChanges="orientation|screenSize"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Drawing Note Activity -->
        <activity
            android:name=".DrawingNoteActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Chat Activity -->
        <activity
            android:name=".ChatActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Calendar Activity -->
        <activity
            android:name=".CalendarActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Calendar Event Detail Activity -->
        <activity
            android:name=".CalendarEventDetailActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Calendar Categories Activity -->
        <activity
            android:name=".CalendarCategoriesActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Calendar Settings Activity -->
        <activity
            android:name=".CalendarSettingsActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Calendar Search Activity -->
        <activity
            android:name=".CalendarSearchActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Calendar Dashboard Activity -->
        <activity
            android:name=".CalendarDashboardActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Calendar Analytics Activity -->
        <activity
            android:name=".CalendarAnalyticsActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- File Manager Activity -->
        <activity
            android:name=".FileManagerActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Settings Activity -->
        <activity
            android:name=".SettingsActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Expense Tracker Activity -->
        <activity
            android:name=".ExpenseTrackerActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Income Tracker Activity -->
        <activity
            android:name=".IncomeTrackerActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Net Worth Dashboard Activity -->
        <activity
            android:name=".NetWorthDashboardActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Subscriptions / Recurring Expenses Activity -->
        <activity
            android:name=".SubscriptionsActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Borrow / Lend Activity -->
        <activity
            android:name=".BorrowLendActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Budget Goals per Category Activity -->
        <activity
            android:name=".BudgetGoalsActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Wallets Activity -->
        <activity
            android:name=".WalletsActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Wallet Detail Activity -->
        <activity
            android:name=".WalletDetailActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Split Groups Activity -->
        <activity
            android:name=".SplitGroupsActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Group Detail Activity -->
        <activity
            android:name=".GroupDetailActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Password Manager Activity -->
        <activity
            android:name=".PasswordManagerActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- ─── Personal Media Vault Activities ─── -->

        <!-- Vault Unlock Screen -->
        <activity
            android:name=".VaultUnlockActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:windowSoftInputMode="adjustPan" />

        <!-- Vault Home Screen -->
        <activity
            android:name=".VaultHomeActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <!-- Vault File Browser -->
        <activity
            android:name=".VaultFileBrowserActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <!-- Vault Album Detail -->
        <activity
            android:name=".VaultAlbumDetailActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <!-- Vault Activity Log -->
        <activity
            android:name=".VaultActivityLogActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <!-- Vault Image Viewer -->
        <activity
            android:name=".VaultImageViewerActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <!-- Vault Video Player -->
        <activity
            android:name=".VaultVideoPlayerActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <!-- Vault Audio Player -->
        <activity
            android:name=".VaultAudioPlayerActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <!-- Vault Document Viewer -->
        <activity
            android:name=".VaultDocumentViewerActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <!-- Vault Import -->
        <activity
            android:name=".VaultImportActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:windowSoftInputMode="adjustResize" />

        <!-- Vault Search -->
        <activity
            android:name=".VaultSearchActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:windowSoftInputMode="adjustResize" />

        <!-- Vault Settings -->
        <activity
            android:name=".VaultSettingsActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <!-- Notes Settings -->
        <activity
            android:name=".NotesSettingsActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Smart File Hub Activities -->
        <activity
            android:name=".SmartFileHubActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <activity
            android:name=".HubInboxActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <activity
            android:name=".HubFileBrowserActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".HubSearchActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".HubDuplicateManagerActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".HubProjectsActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".HubProjectDetailActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".HubFileViewerActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Foreground Service -->
        <service
            android:name=".ConnectionForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Dynamic Bar Overlay Service -->
        <service
            android:name=".DynamicBarService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Notification Mirror Service -->
        <service
            android:name=".NotifMirrorService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
        <!-- Device Admin Receiver (for Lock Screen) -->
        <receiver
            android:name=".MyDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Task Reminder Alarm Receiver -->
        <receiver
            android:name=".TaskReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.prajwal.myfirstapp.ACTION_MARK_COMPLETE" />
                <action android:name="com.prajwal.myfirstapp.ACTION_SNOOZE_15" />
                <action android:name="com.prajwal.myfirstapp.ACTION_SNOOZE_1H" />
            </intent-filter>
        </receiver>

        <!-- Note Reminder Alarm Receiver -->
        <receiver
            android:name=".NoteReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.prajwal.myfirstapp.NOTE_REMINDER" />
                <action android:name="com.prajwal.myfirstapp.DISMISS_REMINDER" />
                <action android:name="com.prajwal.myfirstapp.SNOOZE_REMINDER" />
            </intent-filter>
        </receiver>

        <!-- Expense Notification Receiver (subscriptions, budget alerts, auto-logging) -->
        <receiver
            android:name=".ExpenseNotificationHelper"
            android:exported="false">
            <intent-filter>
                <action android:name="com.prajwal.myfirstapp.ACTION_SUBSCRIPTION_REMINDER" />
                <action android:name="com.prajwal.myfirstapp.ACTION_EXPENSE_LOGGED" />
                <action android:name="com.prajwal.myfirstapp.ACTION_BUDGET_THRESHOLD" />
                <action android:name="com.prajwal.myfirstapp.ACTION_BUDGET_EXCEEDED" />
                <action android:name="com.prajwal.myfirstapp.ACTION_CHECK_RECURRING" />
            </intent-filter>
        </receiver>

        <!-- Calendar Reminder Receiver -->
        <receiver
            android:name=".CalendarReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.prajwal.myfirstapp.CALENDAR_EVENT_REMINDER" />
                <action android:name="com.prajwal.myfirstapp.CALENDAR_DAILY_AGENDA" />
                <action android:name="com.prajwal.myfirstapp.CALENDAR_VIEW_EVENT" />
                <action android:name="com.prajwal.myfirstapp.CALENDAR_SNOOZE_15" />
            </intent-filter>
        </receiver>

        <!-- Meeting Reminder Receiver -->
        <receiver
            android:name=".MeetingReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.prajwal.myfirstapp.ACTION_MEETING_REMINDER" />
                <action android:name="com.prajwal.myfirstapp.ACTION_MEETING_STARTING_NOW" />
                <action android:name="com.prajwal.myfirstapp.ACTION_MEETING_MISSED" />
                <action android:name="com.prajwal.myfirstapp.ACTION_JOIN_MEETING" />
            </intent-filter>
        </receiver>

        <!-- IOU Notification Receiver -->
        <receiver
            android:name=".IouNotificationHelper"
            android:exported="false">
            <intent-filter>
                <action android:name="com.prajwal.myfirstapp.ACTION_IOU_OVERDUE" />
                <action android:name="com.prajwal.myfirstapp.ACTION_IOU_REMINDER" />
                <action android:name="com.prajwal.myfirstapp.ACTION_IOU_SETTLED" />
                <action android:name="com.prajwal.myfirstapp.ACTION_CHECK_IOU" />
            </intent-filter>
        </receiver>

        <!-- Boot Receiver — reschedules task alarms after reboot -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- FileProvider for sharing files with other apps -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
